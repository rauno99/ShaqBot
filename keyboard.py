import cv2

def moveKeyboard():
    while True:

        if cv2.waitKey(10) & 0xFF == ord("o"):
            keyboard.
        if cv2.waitKey(10) & 0xFF == ord("o"):
            keyboard.
        if cv2.waitKey(10) & 0xFF == ord("o"):
            keyboard.
        if cv2.waitKey(10) & 0xFF == ord("o"):
            keyboard.

        if cv2.waitKey(10) & 0xFF == ord("o"):
            break